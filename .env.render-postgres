# PostgreSQL-Only Environment Variables for Render (3 Tables)

# =================================
# SINGLE POSTGRESQL DATABASE SETUP
# 3 COMPREHENSIVE TABLES:
# 1. sales (public schema) - Revenue & order data
# 2. marketing.campaigns (marketing schema) - Campaign performance  
# 3. customers (public schema) - Customer intelligence
# =================================

# PostgreSQL Database (All 3 tables in one database)
CUBEJS_DB_TYPE=postgres
CUBEJS_DB_HOST=your-render-postgres-host.onrender.com
CUBEJS_DB_NAME=analyticsdb
CUBEJS_DB_USER=postgres
CUBEJS_DB_PASS=your-render-postgres-password
CUBEJS_DB_PORT=5432

# Marketing schema configuration (same database, different schema)
CUBEJS_DS_MARKETING_DB_SCHEMA=marketing

# Cube.js Configuration
CUBEJS_API_SECRET=d621c128b8608b38ca344d2720d16e2e745f6cae2592d1ab956506cb348e317d
CUBEJS_EXTERNAL_DEFAULT=true
CUBEJS_SCHEDULED_REFRESH_DEFAULT=true
CUBEJS_DEV_MODE=false

# Render Port (automatically set)
PORT=4000

# =================================
# REACT FRONTEND SERVICE VARIABLES
# =================================

# Point to your Cube.js API service on Render
REACT_APP_CUBEJS_URL=https://your-cubejs-service-name.onrender.com
REACT_APP_CUBEJS_TOKEN=d621c128b8608b38ca344d2720d16e2e745f6cae2592d1ab956506cb348e317d

# =================================
# COMPREHENSIVE ANALYTICS TABLES:
# =================================

# TABLE 1: sales (50+ records)
# - Order data, revenue, products, regions
# - Connected to customers and campaigns
# - Multiple product categories & sales channels

# TABLE 2: marketing.campaigns (10+ campaigns)  
# - Campaign performance, ad spend, conversions
# - Multi-platform (Google, Facebook, Email, etc.)
# - ROI and cost metrics included

# TABLE 3: customers (30+ customers)
# - Customer demographics, segments, LTV
# - Acquisition channels, purchase history
# - Geographic and behavioral data

# =================================
# POWER BI INTEGRATION ENDPOINTS:
# =================================

# Sales Analytics:
# /cubejs-api/v1/load?query={"measures":["Sales.totalRevenue","Sales.count"],"dimensions":["Sales.campaignId","Sales.region","Sales.productCategory"]}

# Marketing Performance:
# /cubejs-api/v1/load?query={"measures":["Marketing.totalAdSpend","Marketing.totalLeads","Marketing.conversions"],"dimensions":["Marketing.campaignName","Marketing.platform"]}

# Customer Intelligence:
# /cubejs-api/v1/load?query={"measures":["Customers.count","Customers.averageLifetimeValue"],"dimensions":["Customers.customerSegment","Customers.acquisitionChannel"]}

# Cross-Table Analytics (Sales + Customers):
# /cubejs-api/v1/load?query={"measures":["Sales.totalRevenue","Customers.count"],"dimensions":["Customers.customerSegment","Sales.productCategory"]}

# =================================
# BENEFITS OF 3-TABLE SETUP:
# =================================

# ✅ Complete business intelligence platform
# ✅ Rich customer segmentation & LTV analysis  
# ✅ Campaign ROI and attribution tracking
# ✅ Cross-table analytics (Sales + Marketing + Customers)
# ✅ Geographic and demographic insights
# ✅ Time-based trend analysis
# ✅ Professional analytics-as-a-service offering